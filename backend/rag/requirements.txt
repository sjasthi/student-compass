# Backend dependencies for StudentCompass RAG application
# Install with: pip install -r requirements.txt

# ── Core RAG stack ─────────────────────────────────────────
llama-index
llama-index-vector-stores-chroma
llama-index-llms-google-genai
llama-index-embeddings-huggingface
llama-index-readers-web
chromadb
sentence-transformers

# ── LLM / Google ───────────────────────────────────────────
google-generativeai
langchain-google-genai          # used by ingest.py and query.py (Flask app only, not eval_optuna)
langchain-core                  # LangChain base classes used by custom LLM wrapper in eval_optuna
langchain-community             # HuggingFaceEmbeddings used by answer_relevancy scorer

# ── Web framework ──────────────────────────────────────────
flask
flask-cors
python-dotenv

# ── Cloud storage ──────────────────────────────────────────
google-cloud-storage

# ── Retry logic (Option 1 — 503 handling) ──────────────────
tenacity

# ── Parameter search + evaluation (Optuna + RAGAS) ─────────
optuna
ragas==0.1.21                   # pinned — 0.2+ collections API does not support Gemini custom LLMs
datasets                        # HuggingFace Datasets, required by ragas
